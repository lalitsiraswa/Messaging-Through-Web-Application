import {ChatpageDirective} from './chatpage.directive';

describe('ChatpageDirective', () =>
{
  it('should create an instance', () =>
  {
    const directive = new ChatpageDirective();
    expect(directive).toBeTruthy();
  });
});
